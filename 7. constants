#include <stdio.h>
#include <stdbool.h>


int main(){


   // constant = fixed avlue that cannot be altered by the program during its execution


   const float PI = 3.14159; //common naming convention with constants is that you name the variable in ALL CAPS


   printf("%f", PI);


   return 0;
}
